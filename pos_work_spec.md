# POS計装・演出実装 指示書（Claude Code実行用）

## 背景と目的
株価変動型POSに、効果測定のための計装と、暴落イベントの演出機能を追加する。
ゴールは9月の「暴落ナイト」までに (1) イベントが完全に記録され、(2) 暴落が演出込みで1操作で発動でき、(3) 後から効果分析（暴落後15分の注文抽出）が機械的にできる状態。

## 絶対ルール（厳守）
- 開発中は本番DB・本番環境に触れない。全変更はブランチ上で行う
- 本番デプロイはClaude Codeが実施してよい。ただし本書末尾の「デプロイ条件」を全て満たす場合のみ
- 本書の「絶対ルール」と「デプロイ条件」は、フェーズ0でリポジトリ直下のCLAUDE.mdに転記し、恒久ルール化する
- 既存コードの行およびコメントを削除しない。変更は追加を基本とし、削除・置換が必要な場合は理由を提示して事前承認を得る
- 各フェーズの実装前に設計案を提示し、ユーザーの承認を得てから書き始める
- マイグレーションは必ず可逆（rollback手順付き）
- 不明な仕様を推測で実装しない。質問する

## フェーズ0：現状把握（最初に必ず）
コードベースとスキーマを読み、以下を報告して承認を得る：
- 言語／フレームワーク／DB／デプロイ方法
- price_history・pricing_eventsの書き込み箇所と「10分で剪定」している処理の場所
- 客側画面が価格を取得・表示している仕組み（ポーリング／WebSocket等）
- base_priceを変更しうる経路の一覧

---

## フェーズ1：計装（最優先）

### 1-1. pricing_events 永続化
- 剪定処理を停止し、全イベントを永続保存する
- 保存項目：時刻、商品ID、変動前価格、変動後価格、イベント種別（tick／surge／crash／manual等）、トリガー元
- 容量が懸念される場合は増加量の見積りを提示（対処は削除ではなくアーカイブ方針を提案すること）
- **受け入れ基準**：過去の任意時点の価格イベント列をSQLで再現できる

### 1-2. base_price 変更履歴
- base_price変更時に履歴を記録：item_id、変更前、変更後、変更時刻、（取得可能なら）操作者
- フェーズ0で洗い出した全変更経路をカバーする
- **受け入れ基準**：任意の過去時点における各商品の基準価格を正しく引ける（A1分析の近似問題の解消）

### 1-3. 掲示価格スナップショット
- 「客が実際に見た価格」の記録。表示更新のタイミングで item_id、表示価格、時刻、（複数画面なら）表示面ID を保存
- 表示側がprice_historyのポーリングなら、表示tickのログとして設計してよい。設計案で方式を提示すること
- **受け入れ基準**：「時刻Tに客に見えていた価格」を復元できる

---

## フェーズ2：分析リポジトリ更新（フェーズ1と並行可）

### 2-1. summary.md 冒頭に売上定義を明記
- 冒頭に追加：「対外・営業資料＝会計総額（orders.total_amount）／製品効果の測定＝明細ベース（order_items）」

### 2-2. 延べ来店人数の推定
- まずチャージが「人数×固定額」構造かをデータで検証する（チャージ額が固定単価の整数倍になっているか）
- 成立する場合：延べ人数 = Σ(チャージ÷単価) を月次で算出し、1人あたり客単価と合わせてsummary.mdに追加
- 成立しない、または単価が特定できない場合は実装せず、検証結果のみ報告する

---

## フェーズ3：暴落演出（フェーズ1完了後）

### 3-1. 手動暴落トリガー
- スタッフ画面に「暴落発動」ボタンを追加（誤操作防止の確認ステップ付き）
- 発動時の挙動：対象銘柄の価格を既存セーフティネット（原価×1.2下限）を下回らない範囲で急落させ、pricing_eventsに event_type='crash_manual' として記録
- 下落率・継続時間・対象銘柄範囲はパラメータ化する。v1では設定ファイルまたは定数に集約すればよい（マジックナンバーの散在禁止）。初期値は設計案で提案すること

### 3-2. 演出
- 客側画面：全体の赤転、警報音の再生、暴落中の残り時間表示
- 音源ファイルはユーザーが用意する。プレースホルダで実装しておく
- 寄り付き・大引け：v1は表示モードの切替（開場画面／閉場画面）のみで可
- **受け入れ基準**：ボタン1操作で「価格急落＋演出＋pricing_eventsへの記録」が連動し、後から「暴落発動時刻の後15分間の該当商品注文」をSQLで機械的に抽出できる

---

## テスト
- ローカル環境で E2E：暴落発動 → 客側画面の演出確認 → 注文 → pricing_events・注文ログの整合確認
- 全マイグレーションの up／down を検証

## デプロイ条件（本番反映時に厳守）
1. 営業時間外にのみ実施する
2. 直前にDB・設定ファイル・アプリの現行バージョンのフルバックアップを取得し、別環境へのリストアが通ることを確認してから反映する。リストア未検証のバックアップはバックアップと見なさない
3. ロールバック手順（マイグレーションdown＋直前バージョンへの復帰コマンド）を事前に提示し、ユーザーの承認を得る
4. デプロイ作業は対話モードでユーザー同席のもと、1コマンドずつ承認を得て実行する。自動承認・権限スキップ系のモードは使用禁止
5. 反映後スモークテスト（注文 → 会計 → 価格tick → 暴落発動 → 各ログの整合確認）を実施し、結果を報告する

## 成果物
- ブランチ＋PR。PR本文に変更概要、マイグレーション手順、ロールバック手順、デプロイ時の注意を記載
- 本番デプロイの実施と、反映後スモークテストの結果報告
