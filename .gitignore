# 依存関係
node_modules/

# ビルド成果物
dist/
dist-ssr/

# 環境変数・秘密情報（.env.example はコミットする）
.env
.env.local
.env.*.local

# ログ
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS・エディタ
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code ローカル設定
.claude/settings.local.json

# 開発ツールの生成物
.superpowers/
raspberrypi.txt
# 開発用 docker compose オーバーライド（ローカルのポート公開など）
docker-compose.override.yml

# 業務データ・機密（publicリポジトリのため厳守：売上/個人情報/認証情報をコミットしない）
# ※gitignoreは行末コメント非対応。コメントは必ず独立行に書く
# 実売上を含む分析レポート
analysis/
# DBバックアップ・ダンプ
backups/
*_dump.sql
*.dump
*.tar
*.tar.gz
# アップロード済み商品画像（業務データ）
server/uploads/
uploads/
# 試合日等のデータ
match_days.csv
# 個人/機密メモ用の慣習拡張子
*.private.md

# 本番接続先(user@host)。公開リポジトリのためコミットしない
deploy/.prod-ssh
